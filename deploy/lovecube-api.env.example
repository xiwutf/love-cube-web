# 复制为服务器上的 /opt/lovecube-api/lovecube-api.env 后填入真实值。
# 不要把真实密钥提交进 Git。该文件由 install-infra workflow 根据 GitHub Secrets 生成。
# systemd EnvironmentFile 语法：KEY=value，不要写 export。

OSS_ENABLED=true
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_ENDPOINT=
OSS_BUCKET_NAME=
OSS_CDN_DOMAIN=
