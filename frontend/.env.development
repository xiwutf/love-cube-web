# 本地开发固定端口（与 vite strictPort 配合，避免自动跳到 5174）
VITE_DEV_PORT=5173

# 开发环境：通过 Vite 代理访问后端（相对路径，由 vite.config.js 的 proxy 转发）
VITE_API_BASE_URL=/admin

# 后端服务地址（仅供 Vite proxy 使用，不注入浏览器）
VITE_BACKEND_ORIGIN=http://localhost:8090
